require 'bundler/setup'
require 'padrino-core/cli/rake'

# For some reason this isn't loaded when running rake
# tasks.. So that's why it's here.
# Ya didn't see nothing..
require 'warden'

PadrinoTasks.use(:activerecord)
PadrinoTasks.use(:database)
PadrinoTasks.init