li
  a
    .cell.one.name
      .hole
        div.num= "#" + hole.number
        div.par_idx
          div= "Par: " + hole.par
          div= "Idx: " + hole.index
    each score in scores
      .cell.two.score 
        div(class=score.result)= score.pretty_score
  .score-keeper
    ul.score-players
    a(href="#")= "Save Scores for Hole #" + hole.number
